"""History page — transaction audit log."""

import streamlit as st


def show_history():
    """Render the transaction history page (handled in main app.py)."""
    st.info("Use the Transaction History page from the sidebar navigation.")
